services:
  database:
    image: postgres:17
    container_name: piedrazul-db
    environment:
      POSTGRES_DB: piedrazul
      POSTGRES_USER: piedrazul
      POSTGRES_PASSWORD: piedrazul
    ports:
      - "5432:5432"
    volumes:
      - piedrazul-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U piedrazul -d piedrazul"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  piedrazul-data:
